import { createClient } from "@/utils/supabase/server";

// - Register other party members using input from the user, then inserts new guests into guests table
// - Should have a ‘group_code’ variable in users