'use client'

import SubmitUser from '@/components/SubmitUser';

const Home: React.FC = () => {
    return (
        <div>
            <h1>Add User</h1>
            <SubmitUser />
        </div>
    );
};

export default Home;
